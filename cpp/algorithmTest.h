#include <vector>

class findKth {
private:
    static int find(std::vector<int>, std::vector<int>, int);
    static int find2(std::vector<int>, std::vector<int>, int);

public:
    static void test();
};
